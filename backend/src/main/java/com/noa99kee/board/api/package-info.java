/**
 * REST API 진입점({@code @RestController})만 둡니다.
 *
 * <p>여기 클래스는 가능한 얇게 유지하고, 검증·트랜잭션·도메인 규칙은 서비스 계층으로 넘기는 것이 일반적입니다. 요청·응답 타입은
 * {@link com.noa99kee.board.dto} 아래 패키지를 import합니다.
 */
package com.noa99kee.board.api;
