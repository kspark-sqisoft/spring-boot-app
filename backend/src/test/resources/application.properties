spring.datasource.url=jdbc:h2:mem:boardtest;DB_CLOSE_DELAY=-1;MODE=PostgreSQL;DATABASE_TO_LOWER=TRUE
spring.datasource.username=sa
spring.datasource.password=
spring.datasource.driver-class-name=org.h2.Driver
spring.jpa.hibernate.ddl-auto=create-drop
spring.jpa.database-platform=org.hibernate.dialect.H2Dialect

app.jwt.access-secret=test_secret_at_least_32_bytes_long_ok!!
app.jwt.access-expiration-minutes=15
app.auth.refresh-cookie-name=refresh_token
app.auth.refresh-expires-days=7
app.auth.refresh-cookie-secure=false
app.uploads.dir=build/test-uploads
app.cors.allowed-origins=
